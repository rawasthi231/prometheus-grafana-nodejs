apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    # url: http://docker.for.mac.host.internal:9090 # For Mac
    url: http://docker.for.win.host.localhost:9090 # For Windows
    basicAuth: false
    isDefault: true
    editable: true
